name: Run Pytest Tests

on:
  push:
    branches: [ main, dev ]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        worker_id: [1, 2, 3, 4]

    steps:
      - uses: actions/checkout@v3

      # Cache pip
      - name: Cache pip packages
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}

      # Cache Playwright browsers
      - name: Cache Playwright browsers
        uses: actions/cache@v3
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/requirements.txt') }}

      # Set up Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # Install dependencies
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install playwright pytest-playwright pytest-split
          playwright install --with-deps

      # Warm-up browser
      - name: Warm up browser launch
        run: |
          python - << 'EOF'
          from playwright.sync_api import sync_playwright
          with sync_playwright() as p:
              p.chromium.launch()
          EOF

      # Worker env vars
      - name: Set worker environment variables
        run: |
          echo "GITHUB_WORKER_ID=${{ matrix.worker_id }}" >> $GITHUB_ENV
          echo "GITHUB_TOTAL_WORKERS=4" >> $GITHUB_ENV
          echo "BASE_URL=https://www.saucedemo.com" >> $GITHUB_ENV

      # Run sliced tests (NO xdist)
      - name: Run sliced parallel tests
        run: |
          pytest -vv \
            --browser chromium \
            --base-url $BASE_URL \
            --disable-warnings \
            --maxfail=1 \
            --splits $GITHUB_TOTAL_WORKERS \
            --group $GITHUB_WORKER_ID \
            --suppress-no-test-exit-code
